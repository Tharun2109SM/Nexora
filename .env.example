# Copy the service-specific examples instead of adding secrets here:
#   apps/web/.env.example -> apps/web/.env.local
#   apps/api/.env.example -> apps/api/.env
#
# Never commit the resulting environment files.
